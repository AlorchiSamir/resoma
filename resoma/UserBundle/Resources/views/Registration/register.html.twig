<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register" id="accueilForm">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'Inscription'|trans({}, 'FOSUserBundle') }}" />
    </div>
</form>